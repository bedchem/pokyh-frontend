<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

# Shared delivery coordination

The legal pages are shared work with `pokyh_learn-frontend` and
`pokyh-backend`. Preserve existing Pokyh legal content, company/address data,
and unrelated routes. Add Learn-specific notices beside the existing notices;
do not replace or delete established content without explicit human approval.

Before editing, inspect `git status`. Do not use destructive Git/file
operations or discard unfamiliar changes. Record material legal-page changes
and verification in `pokyh_learn-frontend/docs/worklog/` without credentials,
personal data, or private reasoning. Before every commit/push, obtain the
human owner's confirmed scope and Git author name/email; do not add automated
co-author trailers.
