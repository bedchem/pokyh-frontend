'use client';

// Firebase has been replaced by the custom Node.js backend.
// This file is kept as a stub to avoid breaking imports.
export const auth = null;
export const db = null;
export const firebaseEnabled = false;
export default null;
